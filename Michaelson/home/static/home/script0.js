document.addEventListener('DOMContentLoaded', () => {
    social = document.querySelector('.social');
    social.style.animationPlayState = 'paused';
    social.onmouseover = () => {
        if (social.animationPlayState === 'paused')
    }
});